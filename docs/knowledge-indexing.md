# Knowledge Indexing MVP

Place `.md` or `.txt` files in `knowledge/`.

Start the backend and open the dashboard. AnalyticsOS auto-discovers supported files and makes them visible under Knowledge Assets.

Search is currently simple keyword matching. Embeddings and PostgreSQL/pgvector will come later.
