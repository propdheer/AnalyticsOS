# Configuration

The frontend reads the backend URL from `frontend/.env.local`.

Default:

```text
NEXT_PUBLIC_ANALYTICSOS_API_URL=http://127.0.0.1:8000
```

Runtime JSON data is stored in `data/`.

Local markdown and text knowledge assets are stored in `knowledge/`.
